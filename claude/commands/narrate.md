---
description: Toggle spoken narration of responses for this session (on|off|status)
argument-hint: on|off|status
allowed-tools: Bash(~/.local/bin/narrate-session:*)
---

Run `~/.local/bin/narrate-session $ARGUMENTS` (use `status` if no argument was given) via the Bash tool, then report its output back in one short line and stop. Do nothing else.
